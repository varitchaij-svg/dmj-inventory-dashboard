# ตรวจความถูกต้องของ Dashboard ทั้งระบบ + แผนแก้ (ส.ค. 2026)

สถานะ: **Phase 0 + 1 + 2 + 3 เสร็จแล้ว** (เหลือ Phase 4 เทสต์เพิ่มเติม · Phase 5 review + deploy)
เจ้าของรัน `debugPurchasePrices()` แล้ว 1 ส.ค. 2026 — ผลอยู่ในหัวข้อ 8 ด้านล่าง
ที่มา: เจ้าของส่งภาพหน้า "ภาพรวม" 2 ใบ (เลือก มิ.ย. 26 / ก.ค. 26) — "เลขบางอันแค่มองก็ผิดแล้ว"
ขอบเขตที่ไล่ดู: `OverviewView` · `StockView` · `TrendsView` · `SeasonView` · `MarginView` ·
`CustomerView` · `DeadStockView` · `abcClassify` (StockCount/FrontStore) · `WarehouseHomeView` ·
ฝั่ง GAS: `buildFullData_` / `aggregateAndWriteSales_` / `readPurchases_` / `syncZortPurchases`

Baseline ก่อนแก้: `npx vitest run` → **781 tests / 23 files ผ่านหมด**

---

## สรุปให้เจ้าของอ่านก่อน (30 วินาที)

| ตัวเลขในภาพ | จริงหรือเปล่า | สาเหตุ |
|---|---|---|
| `↓ 99.9%` ทั้ง มิ.ย. และ ก.ค. | ❌ **ผิด** | เทียบ "ส.ค. 26 ที่เพิ่งผ่านไป 1 วัน" กับ ก.ค. เสมอ ไม่เกี่ยวกับเดือนที่กดเลือก |
| `0 SKU มียอดขาย` (ทั้งที่ขาย 6,063 ชิ้น) | ❌ **ผิด** | backend ไม่เคยส่งค่า `nSold` มาเลย → หน้าเว็บอ่านได้ `undefined` แล้วโชว์ 0 |
| `ยอดขาย/ต้นทุนสต๊อก 1.00×` | ❌ **ไร้ความหมาย** | เอายอดขาย **ทั้งประวัติ 2.5 ปี** หารด้วยมูลค่าสต๊อก **วันนี้** · ที่ได้ 1.00 เป็นเรื่องบังเอิญ |
| `มูลค่าซื้อรวม ฿1` (38 SKU / 11,848 ชิ้น) | ❌ **ผิด** | ราคาต่อหน่วยใน PO เป็น 0 เกือบทุกแถว แต่หน้าเว็บบวกแล้วโชว์เงียบ ๆ |
| `มูลค่าสต๊อก ฿36.23M` | ⚠️ **ใช้ราคาผิดฐาน** | คิดที่ราคาขายปลีกจาก ZORT · ตามที่เจ้าของบอก ต้อง −20% = **฿28.98M** |
| `ยอดขายรวม 1.10M / 1.28M` | ✅ ถูก | กรองตามเดือนที่เลือกจริง |
| `จำนวนชิ้น 6,063 / 7,669` | ✅ ถูก | |
| `คลัง 20.12M + หน้าร้าน 16.11M = 36.23M` | ✅ บวกถูก | (แต่ฐานราคายังผิดตามข้อบน) |

**เรื่องใหญ่ที่ไม่เห็นในภาพแต่อันตรายกว่า**: ทุกที่ที่คำนวณ "เฉลี่ย N เดือนล่าสุด" นับเดือนปัจจุบัน
ที่ยังไม่จบรวมเข้าไปด้วย → ตัวเลข **"ควรสั่งเท่าไร" ในหน้าสต๊อกต่ำกว่าความจริงประมาณ 33%**
(วันนี้ 1 ส.ค. เดือน ส.ค. แทบไม่มียอด แต่ถูกหารเฉลี่ยเท่ากับ มิ.ย./ก.ค.) → **เสี่ยงสั่งของขาด**

---

## 1. บั๊กที่เห็นด้วยตาในภาพ (P0 — แก้ก่อน)

### 1.1 `↓ 99.9%` — เทียบผิดเดือน + เทียบกับเดือนที่ยังไม่จบ

`views-main.jsx:1150-1152`

```js
const prevRev = monthlySeries[monthlySeries.length-2].rev;   // ← เดือนรองสุดท้ายในข้อมูล
const curRev  = monthlySeries[monthlySeries.length-1]?.rev;  // ← เดือนสุดท้ายในข้อมูล
const momDelta = prevRev > 0 ? ((curRev - prevRev) / prevRev * 100).toFixed(1) : null;
```

แล้วถูกใช้ที่ `views-main.jsx:1677-1681` → `deltaVal` → แสดงคู่กับป้าย `subLabel` ที่เป็น
**เดือนที่ผู้ใช้เลือก** (`views-main.jsx:1867-1869`)

ปัญหา 2 ชั้นซ้อนกัน:
1. `momDelta` ไม่สนใจ `activeMonth` เลย — กด มิ.ย. หรือ ก.ค. ก็ได้เลขเดียวกัน
2. เดือนสุดท้ายในข้อมูลคือ **08/2026** ซึ่งวันนี้เพิ่งเข้าวันที่ 1 → `curRev ≈ 0`
   → `(0 − 1,280,000) / 1,280,000 = −99.9%` ตรงกับที่เห็นเป๊ะ

`aggregateAndWriteSales_` (`appsscript_complete.gs:4813-4825`) สร้างคอลัมน์เดือนใหม่ทันที
ที่มีบิลใบแรกของเดือน → เดือนที่ยังไม่จบจะโผล่ใน `monthLabels` เสมอ

**แก้**: คำนวณ delta จาก `activeMonth` เทียบเดือนก่อนหน้า `activeMonth` จริง ๆ ·
ถ้า `activeMonth` = เดือนปัจจุบัน ให้เทียบแบบ **pro-rate** (วันที่ผ่านไป/วันทั้งเดือน)
หรือซ่อน badge แล้วขึ้นป้าย "เดือนนี้ยังไม่จบ (ผ่านไป N/31 วัน)" แทนการโชว์ −99.9%

### 1.2 `0 SKU มียอดขาย`

`appsscript_complete.gs:2372-2384` — `totals` **ไม่มีคีย์ `nSold`** (มีแต่ `totalSoldQty`/`totalSoldRev`)

`views-main.jsx:1310-1313` อ่าน `totals.nSold` → `undefined` → `fmtN(undefined)` = `"0"`
(`ui.jsx:5`)

สังเกตได้ว่าเป็นบั๊กจริง เพราะพอ**เลือกหมวด** โค้ดจะไปเข้า branch ที่คำนวณเองที่
`views-main.jsx:1314-1324` แล้วเลขขึ้นถูก — ผิดเฉพาะตอน "ทุกหมวด"

**แก้**: เพิ่ม `nSold` ใน `totals` ฝั่ง GAS · และเปลี่ยนความหมายให้เป็น
"SKU ที่มียอดขาย **ในช่วงเวลาที่เลือก**" ไม่ใช่ตลอดกาล (ไม่งั้นจะขัดกับ KPI ข้าง ๆ ที่เป็นรายเดือน)

### 1.3 `ยอดขาย / ต้นทุนสต๊อก 1.00×`

`views-main.jsx:1888-1891`

```js
value={stockAgg.val > 0 ? `${(stockAgg.soldRev / stockAgg.val).toFixed(2)}×` : "—"}
```

- ตัวตั้ง `totals.totalSoldRev` = ยอดขาย **สะสมตั้งแต่ ม.ค. 2024** (~2.5 ปี)
- ตัวหาร `totals.totalStockValue` = มูลค่าสต๊อก **ณ วันนี้** และเป็น **ราคาขายปลีก** ไม่ใช่ต้นทุน
- ที่ได้พอดี 1.00× เพราะบังเอิญ (1.2M/เดือน × ~30 เดือน ≈ 36M ≈ มูลค่าสต๊อก) **ไม่ได้ hard-code**

ป้ายเขียนว่า "หมุนเวียนสินค้า" แต่สูตรไม่ใช่ inventory turnover

**แก้**: `turnover = COGS 12 เดือนล่าสุด ÷ มูลค่าสต๊อกที่ราคาทุน` (ต่อปี) ·
ต้องรอข้อ 1.4 (ราคาต้นทุนจริงจาก PO) ก่อน ไม่งั้นก็ยังเป็นตัวเลขปลอมอยู่ดี

### 1.4 `มูลค่าซื้อรวม ฿1`

`views-main.jsx:1344` → `g.cost += (pu.qty||0) * (pu.unitPrice||0)`
`appsscript_complete.gs:7712` → `unitPrice: parseNum_(r[27])`
`appsscript_complete.gs:6650-6653` → `syncZortPurchases` เขียน `row[27] = Number(item.pricepernumber || 0)`

จำนวนชิ้น (คอลัมน์ 26) ออกมาถูก 11,848 · ชื่อ/SKU (คอลัมน์ 24-25) ถูก → **ดัชนีคอลัมน์ไม่เพี้ยน**
เหลือความเป็นไปได้ว่า `pricepernumber` ที่ ZORT คืนมาสำหรับ PO ชุดนี้เป็น 0/ว่าง

⚠️ **ข้อนี้ยังฟันธงสาเหตุไม่ได้จากโค้ดอย่างเดียว — ต้องดูข้อมูลจริงก่อน** (ดู Phase 0)

**แก้ที่ทำได้แน่นอนโดยไม่ต้องรอ**: ห้ามโชว์ยอดรวมเงียบ ๆ เมื่อข้อมูลไม่ครบ →
นับว่ามีกี่ SKU ที่ไม่มีราคา แล้วโชว์ `฿xxx (ไม่มีราคา 36/38 รายการ)` ·
เพิ่ม fallback `totalprice ÷ number` ถ้า `pricepernumber` ว่าง

หน้าที่พังตามกันไปด้วย: **MarginView** ใช้แหล่งราคาเดียวกัน (`views-analytics.jsx:7969-7981`)
→ ถ้า PO ไม่มีราคา ค่า coverage จะต่ำ กำไรที่โชว์ครอบคลุมสินค้าน้อยมาก

### 1.5 มูลค่าสต๊อกต้องคิดที่ราคาขายส่ง (−20%) ← ตามที่เจ้าของสั่ง

`appsscript_complete.gs:2284-2286`

```js
p.stockValue      = p.qty     * p.price;   // p.price = ราคาจาก ZORT (คอลัมน์ I ชีตสต็อก)
p.stockValueWH    = (p.qtyWH    || 0) * p.price;
p.stockValueStore = (p.qtyStore || 0) * p.price;
```

`p.price` มาจาก `applyQtyLocToProduct_` (`appsscript_complete.gs:7862`) = ราคาขายปลีกที่ ZORT sync มา

น่าสนใจ: ระบบ**มี** `COST_RATIO = 0.8` อยู่แล้ว (`appsscript_complete.gs:207`) และใช้กับ
`p.cost` / `p.profit` — แต่ **ไม่เคยถูกใช้กับ `stockValue`** เลย

**ตัวเลขหลังแก้**

| | ตอนนี้ (ปลีก) | หลังแก้ (ขายส่ง −20%) |
|---|---|---|
| รวม | ฿36.23M | **฿28.98M** |
| คลัง | ฿20.12M | ฿16.10M |
| หน้าร้าน | ฿16.11M | ฿12.89M |

⚠️ **จุดที่ต้องคุยให้ชัดก่อนแก้**: "ราคาขายส่ง" ≠ "ราคาทุน"
ราคาขายส่งคือราคาที่เราขายให้ลูกค้าส่ง · ราคาทุนคือราคาที่เราจ่ายซัพพลายเออร์
KPI ข้อ 1.3 ต้องใช้ **ราคาทุน** (จาก PO) ส่วน KPI มูลค่าสต๊อกใช้ **ราคาขายส่ง** ตามที่สั่ง
→ ต้องเป็นคนละตัวเลข ไม่ใช่ตัวคูณเดียวกัน

จุดอื่นที่คูณ `price` ดิบ ๆ แล้วเรียกว่า "มูลค่า" และต้องแก้ให้เป็นฐานเดียวกัน:
`views-main.jsx:1436` (deadStock), `:4882` (slowMovers), `:5360` / `:5366` (TrendsView)

---

## 2. บั๊กเชิงระบบ: "เดือนปัจจุบันที่ยังไม่จบ" ถูกนับเป็นเดือนเต็ม (P0)

ต้นเหตุเดียวกับข้อ 1.1 แต่ลามไปทั่วแอป ทุกที่ที่เขียน "N เดือนล่าสุด"

| ที่ | โค้ด | ผลกระทบ |
|---|---|---|
| **StockView "ควรสั่ง"** | `views-main.jsx:4854-4865` `m.slice(-3)/3` | 🔴 **ควรสั่งต่ำกว่าจริง ~33% → สั่งของขาด** |
| StockView `overstocked` | `:4885-4888` `monthsLeft > 12` | ธงสินค้าค้างขึ้นเกินจริง |
| StockView `declining` | `:4891-4909` early/late half | "ยอดตก" ปลอม |
| Overview `velocity` | `:1400-1415` "สต๊อกพอกี่วัน" | บอกว่าพอนานกว่าจริง |
| Overview `forecast` | `:1570-1661` regression 6 เดือน | จุดสุดท้ายเกือบ 0 กดเส้นเทรนด์ลง → พยากรณ์ต่ำ |
| TrendsView | `:5292-5314` early/late half | "มาแรง"/"เสี่ยงหาย" ผิดกลุ่ม |
| CategoryView ป้าย "กำลังมาแรง 📈" | `:3191-3198` | ป้ายผิดบนการ์ดสินค้า |
| SeasonView `yrsOf(m)` | `views-analytics.jsx:8239-8249` | เฉลี่ยข้ามปีของเดือนปัจจุบันต่ำเกิน |

**แก้ที่เดียวจบ**: helper กลาง เช่น `completeMonths(monthLabels)` ที่ตัดเดือนปัจจุบันออก
แล้วให้ทุกการคำนวณ "เฉลี่ยย้อนหลัง / เทรนด์ / พยากรณ์" ใช้ตัวนี้
· กราฟและ KPI ยัง**โชว์**เดือนปัจจุบันได้ แต่ติดป้าย "ยังไม่จบเดือน" กำกับ
· ค่าคงที่ตายตัวที่ต้องล้างทิ้งด้วย: `soldQty/5` (`views-main.jsx:4858`) และคำอธิบาย
"ตลอด 5 เดือน" (`:5373`) — ข้อมูลจริงตอนนี้ยาวกว่า 5 เดือนมาก

---

## 3. ความไม่สอดคล้อง — เลขเดียวกันคนละหน้าไม่ตรงกัน (P1)

1. **MTO / "ไม่มีรหัสสินค้า" ถูกนับบ้างไม่นับบ้าง**
   `views-main.jsx:1310-1325` — "ทุกหมวด" ใช้ `totals.*` ที่รวม MTO + ไม่มีรหัส
   แต่พอเลือกหมวดกลับตัด MTO ออก → บวกทุกหมวดแล้วไม่เท่ากับ "ทุกหมวด"
   `sellable` (`:1308`) ก็ไม่ตัด "ไม่มีรหัสสินค้า" ทั้งที่ `topSellers`/`topByCategory` ตัด

2. **MarginView คิดกำไรจากราคาป้าย ไม่ใช่ราคาที่ขายได้จริง** ← เกี่ยวกับ −20% โดยตรง
   `views-analytics.jsx:7997-8008`
   ```js
   unitMargin = price - cost;          // price = ราคาปลีกจาก ZORT
   profit     = unitMargin * soldQty;
   ```
   ถ้าขายส่ง −20% กำไรที่โชว์ **เกินจริงเต็ม ๆ 20% ของราคาขาย**
   → ต้องเป็น `profit = soldRev − cost × soldQty` (`soldRev` คือเงินที่เข้าจริง)

3. **`soldQty` บวก `soldQtyUnscanned` แต่ `soldRev` ไม่บวก** (`appsscript_complete.gs:2277-2281`)
   → ของที่ถูกดึงไปทำงาน MTO ทำให้ "กำไร/ชิ้น × จำนวน" ใน MarginView เพี้ยน

4. **ยอดขายรวมสองหน้าไม่มีทางตรงกัน**
   Overview รวมจาก `list[].totalprice` (รายบรรทัดสินค้า) ·
   CustomerView รวมจาก `order.amount` (ยอดบิล รวมค่าส่ง/ส่วนลด)
   → ต้องมีหมายเหตุบอกผู้ใช้ ไม่งั้นจะคิดว่าระบบพัง

5. **`abcClassify` ใช้ยอดขายทั้งประวัติ** (`views-analytics.jsx:1063-1078`)
   ของที่ขายดีเมื่อ 2 ปีก่อนยังเป็นคลาส A → คิว "ควรนับก่อน"/"ควรเช็คก่อน" จัดลำดับผิด
   → ควรใช้หน้าต่าง 12 เดือนล่าสุด

6. **"ของเข้าใหม่ 30 วัน" ไม่ขยับตามเดือนที่เลือก** (`views-main.jsx:1331-1361`)
   ตั้งใจให้เป็น 30 วันล่าสุดเสมอ แต่ไปวางอยู่ใต้ตัวกรองเดือน → คนอ่านสับสน
   (สังเกตในภาพ: กด มิ.ย. กับ ก.ค. บล็อกนี้เหมือนกันเป๊ะ)

---

## 4. ส่วนที่ตรวจแล้วถูกต้อง ✅

- `sumRev` / `sumQty` กรองตามเดือนที่เลือกจริง (`views-main.jsx:1140-1149`)
- คลัง + หน้าร้าน = ยอดรวม (บวกถูก แค่ฐานราคาผิด)
- `buildYoYSeries` เทียบปีต่อปี ตรรกะถูก (`views-main.jsx:954-`)
- `quarterDelta` เทียบไตรมาสก่อนหน้า ข้ามปีถูก (`views-main.jsx:1664-1675`)
- `abcClassify` แบ่ง A/B/C ด้วย cumulative **ก่อนบวกตัวเอง** ถูกต้องตามหลัก Pareto
- `SeasonView` หาเดือนถัดไป `(getMonth()+1)%12+1` ถูก รวมกรณี ธ.ค.→ม.ค.
- `parseCheckDateMs` จัดการปี พ.ศ. ถูก
- `expandMonthlyCompact` กางข้อมูลย่อกลับเป็นเต็มถูกต้อง (มี test คุมอยู่)

---

## 5. แผนลงมือ — 6 เฟส + โมเดลที่ใช้

> หลักการเลือกโมเดล: อะไรที่**แตะตัวเลขเงิน/สต็อก หรือมีทางเลือกทางธุรกิจ** ใช้ **Opus 5**
> · อะไรที่**รู้คำตอบแล้ว เหลือแค่ไล่แก้ให้ครบทุกจุด** ใช้ **Sonnet 5** (เร็ว/ถูกกว่า) ·
> งานข้อความ/ป้ายล้วน ใช้ **Haiku 4.5**

### Phase 0 — ยืนยันกับข้อมูลจริงก่อน (บล็อกข้อ 1.3 + 1.4)
| งาน | โมเดล | ทำไม |
|---|---|---|
| เขียน `debugPurchasePrices()` ใน GAS: log 20 แถวล่าสุดของชีต "รายการซื้อสินค้า" + ยิง `GetPurchaseOrders` ดู `list[]` ของจริงว่า `pricepernumber` มีค่าไหม | **Opus 5** (agent `zort`) | ต้องอ่าน response จริงแล้วตัดสินว่าจะ fallback ไปฟิลด์ไหน — เดาแล้วแก้ผิดจะพังเงียบ |

ผลลัพธ์ที่ต้องได้: ราคาทุนจริงมาจากฟิลด์ไหน · ถ้า ZORT ไม่มีให้เลย ต้องตัดสินใจว่าจะ
ใช้ราคาทุนโดยประมาณ หรือปิด KPI turnover ไปเลย (ดีกว่าโชว์เลขปลอม)

**เจ้าของต้องรัน `debugPurchasePrices()` ใน GAS editor เอง 1 ครั้ง** (clasp push ไม่รันให้)

### Phase 1 — P0 บนหน้าภาพรวม (แก้ 4 เลขในภาพ) ✅ COMPLETED
| งาน | สถานะ |
|---|---|
| 1.1 `momDelta` อิงเดือนที่เลือก + จัดการเดือนที่ยังไม่จบ | ✅ เสร็จ |
| 1.2 `nSold` ("0 SKU มียอดขาย") | ✅ เสร็จ (มาเองตอนทำ Phase 3 ข้อ 3.1 — ไม่ต้องแก้ GAS) |
| 1.3 KPI turnover | ✅ **ถอด KPI ออก** ตามข้อ 7.3 (Phase 0 ยืนยันว่าไม่มีราคาทุน) |
| 1.4 การ์ด "ของเข้าใหม่" ไม่โชว์ยอดรวมที่ข้อมูลไม่ครบ + fallback ราคา | ✅ เสร็จ |
| 1.5 `stockValue` ใช้ราคาขายส่ง −20% | ✅ เสร็จ |

**1.1 `momDeltaOf(monthlySeries, activeMonth, currentMonthKey)`** (views-main.jsx, pure)
- เทียบ **เดือนที่ผู้ใช้เลือก** กับ **เดือนปฏิทินก่อนหน้าเดือนนั้น** (ไม่ใช่ 2 เดือนท้าย array)
- เดือนก่อนหน้าไม่มีในข้อมูล/ยอด 0 → คืน `null` (ไม่ข้ามไปเทียบเดือนที่ไกลกว่า)
- **เดือนปัจจุบันที่ยังไม่จบ → คืน `null` ไม่โชว์ % เลย** · KPI ขึ้นป้าย
  "ยังไม่จบเดือน (N/M วัน)" อธิบายแทน — ดีกว่าโชว์ `↓99.9%` ที่ไม่จริง

**1.3 ถอด KPI "ยอดขาย / ต้นทุนสต๊อก (หมุนเวียนสินค้า)" ออก**
- Phase 0 ยืนยันว่าไม่มีราคาทุนให้คำนวณ turnover จริง และเจ้าของเคาะว่า
  "โชว์เลขที่คำนวณจากค่าสมมติ อันตรายกว่าไม่โชว์" (ข้อ 7.3)
- แถว KPI ของ owner เหลือ 3 ใบ → `row-4` เป็น `row-3` + CSS เฉพาะ `#ov-kpi.row-3`
  ให้จอเล็กเป็น **2 คอลัมน์** (ถ้าปล่อยตาม `.row-3` เดิมจะกลายเป็น 1 คอลัมน์ = 3 แถวยาวบนมือถือ)
- มีคอมเมนต์ในโค้ดบอกวิธีเอากลับมา (สูตร COGS 12 เดือน ÷ สต๊อกที่ราคาทุน + ป้าย "ครั้ง/ปี")

**1.4 การ์ด "ของเข้าใหม่ 30 วัน"**
- มีราคาบางส่วน → โชว์ยอดรวม **พร้อมป้ายสีส้ม "ไม่มีราคา n/N รายการ"**
- ไม่มีราคาเลย → **ไม่โชว์ตัวเลข** เปลี่ยนเป็น "ยังไม่มีราคาทุน — ใบสั่งซื้อจาก ZORT
  ไม่ได้กรอกราคาต่อหน่วยมา" (เลขที่ผิดชัด ๆ ทำให้ทั้งการ์ดดูเชื่อถือไม่ได้)
- `syncZortPurchases` เพิ่ม fallback `totalprice ÷ number` เมื่อ `pricepernumber` ว่าง
  (Phase 0 บอกว่าวันนี้ยังไม่ช่วย เพราะ ZORT ให้ 0 ทั้งคู่ — กันไว้สำหรับ PO ที่กรอกแต่ยอดรวม)
- MarginView เพิ่มแบนเนอร์ "⚠️ ยังคำนวณกำไรไม่ได้" + วิธีแก้ เมื่อ coverage = 0

**1.5 ราคาขายส่ง**
- GAS: `wholesaleRatio_()` อ่าน Script Property `WHOLESALE_RATIO` (sanitize 0 < x ≤ 1,
  ไม่งั้น 0.8) → คูณเข้า `p.stockValue` / `stockValueWH` / `stockValueStore` ที่เดียว
  · **ห้ามยุบรวมกับ `COST_RATIO`** (ราคาทุน ≠ ราคาขายส่ง) — มี meta-test คุมไว้
- ส่งอัตราลงมาที่ `totals.wholesaleRatio` → frontend อ่านค่าเดียวกัน (`wholesaleRatioOf`)
  **ห้าม hard-code 0.8 ฝั่งเว็บ** วันเปลี่ยนส่วนลดสองฝั่งจะไม่ตรงกันเงียบ ๆ
- `stockValuesOf(p, ratio)` = ตัวกลางฝั่งเว็บ ใช้ค่าจาก GAS ก่อนเสมอ คำนวณเองเฉพาะตอน
  ไม่มี (ข้อมูล preview จากไฟล์อัปโหลด) · ใช้ที่ KPI ภาพรวม · "เงินจม" (สต๊อกตาย) ·
  KPI สต๊อกในหน้าสินค้า&สั่ง (ติดป้าย "· ราคาขายส่ง")
- KPI มูลค่าสต๊อกมีบรรทัดแรกบอกฐาน: **"ราคาขายส่ง (ปลีก −20%)"** (% คำนวณจากอัตราจริง)
- จุดที่คูณ `price` ดิบแล้ว**ไม่ได้โชว์เป็นเงิน** (sort key ของ slowMovers/TrendsView)
  ไม่ต้องแก้ — คูณค่าคงที่ไม่เปลี่ยนลำดับ

**เทสต์เพิ่ม**: `tests/wholesale-ratio.test.js` (eval `wholesaleRatio_` จริงจาก `.gs` + meta-test
ว่ายังคูณเข้า stockValue จริง / ยังส่งลง payload / ไม่เอา COST_RATIO มาปน) ·
`tests/dashboard-metrics.test.js` เพิ่มหัวข้อ `momDeltaOf` + มูลค่าสต๊อกขายส่ง
→ รวม **901 tests / 26 files ผ่านหมด**

### Phase 2 — ล้าง "เดือนที่ยังไม่จบ" ทั้งระบบ ✅ COMPLETED
| งาน | สถานะ | หมายเหตุ |
|---|---|---|
| ออกแบบ helper `completeMonths()` + ตัดสินว่าจุดไหนควรตัด/ไม่ตัด | ✅ เสร็จ | ฟังก์ชั่นตัวแม่กรอง monthly array โดยไม่รวมเดือนปัจจุบัน |
| ไล่แก้ call site ทั้ง 8 จุด | ✅ เสร็จ | **StockView** suggestedQty (ควรสั่ง) · **OverviewView** momMovers + velocity + deadStock |

**ผลลัพธ์**:
- `completeMonths()` helper ที่ filter monthly data โดยแยกเดือนปัจจุบัน (today's month key)
- **StockView**: "ควรสั่ง" ใช้ 3 เดือนสมบูรณ์แทน 3 เดือนล่าสุด (รวมเดือนที่ยังไม่จบ)
- **OverviewView**:
  - `momMovers`: เปรียบเทียบเดือนสมบูรณ์ 2 เดือนล่าสุดแทนรวมเดือนที่ยังไม่จบ
  - `velocity`: ใช้ completeMonthsList แทน months
  - `deadStock`: ดึงข้อมูลจาก 2 เดือนปฏิทินจริงที่เสร็จแล้ว (offsets [1,2] → [07,06] แทน [08,07])
- ทั้งหมด 781 tests ยังผ่านหมด
- commit: `b108c0e`

### Phase 3 — ความสอดคล้อง (P1) ✅ COMPLETED
| งาน | สถานะ |
|---|---|
| MarginView เปลี่ยนมาใช้ `soldRev` (ข้อ 3.2 + 3.3) | ✅ เสร็จ |
| รวมกติกา MTO / "ไม่มีรหัสสินค้า" ให้เหมือนกันทุกหน้า (ข้อ 3.1) | ✅ เสร็จ |
| `abcClassify` ใช้หน้าต่าง 12 เดือน (ข้อ 3.5) | ✅ เสร็จ |
| ป้ายกำกับ/หมายเหตุ: "ยังไม่จบเดือน", "ยอดบิล vs ยอดสินค้า", "30 วันล่าสุด ไม่ขึ้นกับเดือนที่เลือก" | ✅ เสร็จ |

**ผลลัพธ์ (ทั้งหมดฝั่ง frontend ไม่ต้องแตะ GAS / ไม่ต้องให้เจ้าของรันอะไร)**

1. **`isCountableProduct(p)`** (views-main.jsx ข้าง `completeMonths`) = กติกากลางเดียว
   `!isMTO && cat && cat !== "ไม่มีรหัสสินค้า"` (ตรงกับที่ StockView/TrendsView/topSellers ใช้อยู่แล้ว)
   · `stockAgg` เลิกอ่าน `data.totals` ทิ้งทั้งก้อน → คำนวณด้วย **ลูปเดียว กติกาเดียว** ทั้งตอน
   "ทุกหมวด" และ "เลือกหมวด" → บวกทุกหมวดแล้วเท่ากับ "ทุกหมวด" แล้ว
   · **ผลพลอยได้: ข้อ 1.2 (`0 SKU มียอดขาย`) หายไปเอง** — ไม่ต้องรอ GAS ส่ง `nSold` มา
   เพราะนับจากสินค้าจริงตรงนี้ และนับตาม**ช่วงเวลาที่เลือก** ให้ตรงกับตัวเลขที่มันอยู่ใต้
   · ส่วนที่ถูกตัดไม่ได้หายเงียบ — KPI มูลค่าสต๊อกมีบรรทัด "(ไม่รวมงานจัดพิเศษ/ไม่มีรหัส N รายการ ฿X)"
   · ปุ่มเลือกหมวดในหน้าภาพรวมตัดหมวด MTO/"ไม่มีรหัสสินค้า" ออก (เดิมกดแล้ว KPI เป็น 0 ทั้งแถบ)
2. **`marginRowCore(p, cost)`** (views-analytics.jsx) = pure function ตัวใหม่ที่ MarginView ใช้
   · `revQty = soldQty − soldQtyUnscanned` (ของที่เบิกไปงาน MTO ไม่มีเงินเข้าที่นี่)
   · `profit = soldRev − cost × revQty` · `marginPct = profit / soldRev`
   · คอลัมน์ "ขาย/ชิ้น" → **"ขายจริง/ชิ้น"** (`soldRev ÷ revQty`) + โชว์ราคาป้ายตัวเล็กเทียบให้
     เมื่อต่างกันเกิน 2% · คอลัมน์ "ขายได้" โชว์ `revQty` + "+N เบิก MTO" แยกบรรทัด
   · สินค้าที่ถูกเบิกไป MTO ล้วน (ยอดขาย 0) ไม่เข้าตาราง แต่มีบรรทัดสรุปบอกว่าตัดไปกี่ตัว/กี่ชิ้น
   · ทุนจาก PO ที่เป็น 0 ถือว่า "ไม่รู้ต้นทุน" (null) ไม่ใช่กำไร 100% — ต่อกับข้อ 1.4 โดยตรง
3. **`abcWindowRev` + `ABC_WINDOW_MONTHS = 12`** — `abcClassify(products[, windowMonths])`
   จัดคลาสจากยอด **12 เดือนปฏิทินล่าสุดที่จบแล้ว** (ตัดเดือนปัจจุบันออก)
   · ตัดด้วย**เดือนปฏิทินจริง** ไม่ใช่ `slice(-12)` ท้าย array — กันวันที่ `p.monthly` กลายเป็น
   sparse แล้วไปหยิบยอดเมื่อ 2 ปีก่อนมานับเป็น "12 เดือนล่าสุด" เงียบ ๆ
   · ไม่มี array รายเดือน → fallback `soldRev` สะสม (พฤติกรรมเดิม) จึงไม่พังกับ payload เก่า
   · คิว "ควรนับก่อน"/"ควรเช็คก่อน" มีบรรทัดกำกับว่า A/B/C วัดจาก 12 เดือนล่าสุด
4. **ป้ายกำกับ**: KPI เดือนปัจจุบันต่อท้าย "· ยังไม่จบเดือน (N/M วัน)" · การ์ด "ของเข้าใหม่ 30 วัน"
   ติดชิป "🕒 30 วันล่าสุดเสมอ · ไม่ขึ้นกับช่วงเวลาที่เลือก" · CustomerView บอกว่าเป็น **ยอดทั้งบิล**
   (รวมค่าส่ง/ส่วนลด) จึงไม่เท่ากับ "ยอดขายรวม" หน้าภาพรวมที่บวกจากรายการสินค้า

**เทสต์**: `tests/dashboard-metrics.test.js` (49 เคส) — มีเคส "วันนี้เป็นวันที่ 1 ของเดือน" ครบ ·
`tests/helpers.js` เพิ่ม `completeMonths`/`currentMonthInfo`/`isCountableProduct`/`marginRowCore`/
`abcWindowRev` พร้อม landmark ใน `drift-guard.test.js` · รวม **863 tests / 25 files ผ่านหมด**
(เดิม 814/24)

### Phase 4 — เทสต์
| งาน | โมเดล |
|---|---|
| `tests/dashboard-metrics.test.js` — คุม momDelta, nSold, stockValue ขายส่ง, turnover, completeMonths, กำไรจาก soldRev | **Sonnet 5** (agent `tester`) |
| อัปเดต `tests/helpers.js` + `drift-guard` ให้ครบตาม convention | **Sonnet 5** |

ต้องมีเคสที่ **"วันนี้เป็นวันที่ 1 ของเดือน"** โดยเฉพาะ — เป็นวันที่บั๊กนี้แสดงตัวแรงที่สุด

### Phase 5 — ตรวจก่อน deploy
| งาน | โมเดล |
|---|---|
| review diff ทั้งหมด + `npx vitest run` + `tests/browser/run.cjs` | **Opus 5** (agent `reviewer`) |
| merge เข้า `master` (Cloudflare + GAS auto-deploy) | **Opus 5** (agent `deploy`) |

---

## 6b. ตัวเลขที่เจ้าของจะเห็นเปลี่ยนไปหลัง deploy (สรุปสั้น)

| ที่ | ก่อน | หลัง |
|---|---|---|
| KPI มูลค่าสต๊อกคงเหลือ | ฿36.23M (ราคาปลีก) | **~฿28.98M** + บรรทัด "ราคาขายส่ง (ปลีก −20%)" |
| ↳ คลัง / หน้าร้าน | ฿20.12M / ฿16.11M | ~฿16.10M / ~฿12.89M |
| ↳ บรรทัดเพิ่ม | — | "(ไม่รวมงานจัดพิเศษ/ไม่มีรหัส N รายการ ฿X)" |
| `↓ 99.9%` ใต้ยอดขายรวม | โชว์ทุกเดือนที่กด | หายไปเมื่อกดเดือนปัจจุบัน · เดือนอื่นได้ % ของเดือนนั้นจริง |
| `0 SKU มียอดขาย` | 0 เสมอ | จำนวนจริงตามช่วงที่เลือก |
| KPI "ยอดขาย / ต้นทุนสต๊อก 1.00×" | มี | **ไม่มีแล้ว** (เหลือ KPI 3 ใบ) |
| การ์ดของเข้าใหม่ "มูลค่าซื้อรวม ฿1" | ฿1 | "ยังไม่มีราคาทุน — ใบสั่งซื้อจาก ZORT ไม่ได้กรอกราคาต่อหน่วยมา" |
| หน้ากำไรขั้นต้น | ตัวเลขกำไรที่คิดจากราคาป้าย | แบนเนอร์บอกว่าคำนวณไม่ได้ + วิธีแก้ (กรอกราคาใน PO) |
| หน้าสินค้า&สั่ง KPI สต๊อก | มูลค่าราคาปลีก | ราคาขายส่ง + ป้าย "· ราคาขายส่ง" |
| คิว "ควรนับ/ควรเช็คก่อน" | ABC จากยอดสะสม 2.5 ปี | ABC จาก 12 เดือนล่าสุด (ลำดับเปลี่ยน) |

**ไม่ต้องตั้งค่าอะไรก็ใช้ได้ทันที** · อยากเปลี่ยนอัตราขายส่งจาก 20% เป็นค่าอื่น →
ตั้ง Script Property `WHOLESALE_RATIO` (เช่น `0.75` = −25%)

---

## 6. ลำดับความสำคัญถ้าเวลาไม่พอ

1. **Phase 2 (ควรสั่งต่ำกว่าจริง 33%)** — กระทบเงินมากที่สุด ของขาด = ขายไม่ได้
2. **Phase 1 ข้อ 1.5 (มูลค่าสต๊อก −20%)** — ตัวเลขที่เจ้าของสั่งตรง ๆ
3. **Phase 1 ข้อ 1.1 + 1.2** — เลขที่ "มองก็รู้ว่าผิด" ทำให้ทั้งแดชบอร์ดดูเชื่อถือไม่ได้
4. Phase 0 + 1.3 + 1.4 — ต้องรอข้อมูลจริง
5. Phase 3 — ความสอดคล้อง (ไม่มีใครตายวันนี้ แต่ปล่อยไว้จะกลับมาเป็นคำถามซ้ำ)

---

## 7. ข้อตัดสินใจของเจ้าของ (เคาะแล้ว 1 ส.ค. 2026) ✅

1. **มูลค่าสต๊อกคงเหลือ → โชว์ราคาขายส่งอย่างเดียว**
   ตัวเลขเดียว ฿28.98M (คลัง ฿16.10M / หน้าร้าน ฿12.89M)
   · ใต้ตัวเลขมีหมายเหตุเล็ก ๆ ว่า **"ราคาขายส่ง (ปลีก −20%)"** ให้รู้ฐานที่ใช้
   · **ไม่ต้องโชว์ราคาปลีกคู่กัน ไม่ต้องมีปุ่มสลับ** — การ์ดต้องอ่านจบในแวบเดียว
   · จุดอื่นที่คูณ `price` ดิบ ๆ แล้วเรียกว่า "มูลค่า" ต้องเปลี่ยนมาใช้ฐานเดียวกันหมด:
     `views-main.jsx:1436` (deadStock), `:4882` (slowMovers), `:5360`, `:5366` (TrendsView)

2. **ตัวเลข 20% → เก็บเป็น Script Property `WHOLESALE_RATIO` (default `0.8`)**
   · ส่งค่าลงมากับ payload (เช่นใน `_debug` หรือ `totals`) ให้ frontend ใช้ค่าเดียวกัน
     ห้าม hard-code 0.8 ซ้ำฝั่งเว็บ — ไม่งั้นวันที่เปลี่ยนส่วนลด สองฝั่งจะไม่ตรงกันเงียบ ๆ
   · **ห้ามไปแก้ `COST_RATIO` เดิมให้เป็นตัวเดียวกัน** — คนละความหมาย
     (`COST_RATIO` ใช้กับ `p.cost`/`p.profit`, ตัวใหม่ใช้กับมูลค่าสต๊อก)
   · sanitize ค่าที่อ่านมา: ต้องเป็นตัวเลข 0 < x ≤ 1 ไม่งั้น fallback 0.8

3. **KPI "หมุนเวียนสินค้า" → ถ้า Phase 0 พบว่า ZORT ไม่ให้ราคาทุน PO ให้ซ่อน KPI ไปเลย**
   · ไม่ประมาณจากราคาขายส่ง — โชว์เลขที่คำนวณจากค่าสมมติแล้วเจ้าของเอาไปตัดสินใจ
     อันตรายกว่าไม่โชว์
   · ซ่อนแล้วช่อง KPI เหลือ 3 ใบ ต้องเช็คว่า grid ไม่เพี้ยน (`row-4` → `row-3`)
   · ถ้า Phase 0 พบว่า**มี**ราคาทุน → ทำ KPI ตามสูตร `COGS 12 เดือนล่าสุด ÷ มูลค่าสต๊อกที่ราคาทุน`
     และเปลี่ยนป้ายเป็น "หมุนเวียนสินค้า (ครั้ง/ปี)" ให้ตรงกับหน่วยจริง

### สิ่งที่เจ้าของต้องทำเองใน GAS editor (clasp push ไม่รันให้)
- Phase 0: รัน **`debugPurchasePrices()`** 1 ครั้ง แล้วส่ง log กลับมา
- Phase 1: ตั้ง Script Property **`WHOLESALE_RATIO`** (ถ้าอยากได้ค่าอื่นนอกจาก 0.8)

---

## 8. Phase 0 — ✅ เจ้าของรันแล้ว 1 ส.ค. 2026 · ผลลัพธ์ + สิ่งที่ทำตาม

**สรุปผล: ต้นเหตุอยู่ฝั่ง ZORT ไม่ใช่ฝั่งชีต — ใบสั่งซื้อไม่มีราคาทุนมาตั้งแต่ต้นทาง**

| # | คำถาม | คำตอบจาก log |
|---|---|---|
| 1 | ชีตมีราคาจริงกี่ % | **1 / 39 แถว (3%)** ในช่วง 30 วัน · qty รวม 11,848 ตรงกับการ์ด · มูลค่ารวม ฿1 ตรงกับการ์ด |
| 2 | คอลัมน์เลื่อนหรือเปล่า | **ไม่เลื่อน** — header ตรงทุกช่อง (24=รหัสสินค้า, 26=จำนวน, 27=ราคาต่อหน่วย) และแถวเก่าปี 2023 มีราคาอยู่ที่คอลัมน์ 27 ปกติ (400 / 5000) |
| 3 | ใครเขียนแถวพวกนี้ | **`{"สั่งซื้อ": 39}`** = มาจาก `syncZortPurchases` ล้วน ไม่มีแถวที่สร้างในแอป |
| 4 | ZORT ให้ราคามาไหม | **ไม่ให้** — `pricepernumber > 0` เพียง **3 / 91 item** · `totalprice > 0` ก็ **3 / 91** (ชุดเดียวกัน) · JSON เต็มของ item แรกเป็น 0 ทุกฟิลด์ราคา (`pricepernumber_pretax`/`_vat`/`totalprice*`/`discount*`) |

→ ตกทางแยก **"ไม่มีราคาทั้ง 2 ฟิลด์"** ในตารางด้านล่าง · ลงมือตามนั้นแล้วใน Phase 1:
- **ซ่อน KPI หมุนเวียนสินค้า** (ข้อ 1.3) — ถอดออกจริง ไม่ประมาณจากราคาขายส่ง
- **การ์ด "ของเข้าใหม่"** ไม่โชว์บรรทัดมูลค่าเมื่อไม่มีราคาเลย (ข้อ 1.4)
- **MarginView** โชว์ coverage ตามจริง + แบนเนอร์บอกว่าทำไมคำนวณไม่ได้ และต้องทำอะไรต่อ
- ใส่ fallback `totalprice ÷ number` ไว้แล้ว — วันนี้ยังไม่ช่วย (ZORT ให้ 0 ทั้งคู่)
  แต่พอเริ่มกรอกราคาใน PO ตัวเลขจะขึ้นเองโดยไม่ต้องแก้โค้ด

**สิ่งที่เจ้าของทำได้ถ้าอยากได้ตัวเลขกำไร/หมุนเวียนกลับมา**: กรอก **"มูลค่าต่อหน่วย"**
ในใบสั่งซื้อฝั่ง ZORT (ตอนนี้เว้นว่างไว้ทุกใบ) → รอ `syncZortPurchases` รอบถัดไป →
หน้ากำไรขั้นต้นและการ์ดของเข้าใหม่จะมีตัวเลขเองทันที ไม่ต้องแก้อะไรในแอป

---

### บันทึกเดิม (ตอนเขียนโค้ด Phase 0 — เก็บไว้อ้างอิงวิธีตรวจ)

เพิ่ม **`debugPurchasePrices()`** ใน `appsscript_complete.gs`
(section 4b ถัดจาก `exploreZortPurchases`) · **อ่านอย่างเดียว ไม่แก้ชีต ไม่เขียนอะไรกลับ ZORT**
· ไม่ log header ที่มี `apikey`/`apisecret` · ชื่อไม่มี `_` ต่อท้าย จะได้โผล่ใน dropdown (บทเรียนข้อ 1)

**ตอบ 4 คำถาม:**

| # | คำถาม | วิธีตอบใน log |
|---|---|---|
| 1 | ชีตมีราคาจริงกี่ % ในช่วง 30 วัน | นับ `แถวที่มีราคา > 0 : n / N` + ผลรวม qty/มูลค่า เทียบกับที่การ์ดโชว์ (11,848 / ฿1) ตรง ๆ |
| 2 | คอลัมน์เลื่อนหรือเปล่า | dump header 2 แถวแรก + ค่าดิบ**ทั้งแถว**ของ 2 แถวสุดท้ายพร้อม index → เห็นทันทีถ้าราคาไปอยู่คอลัมน์อื่น |
| 3 | แถวพวกนี้ใครเขียน | นับแยกตามคอลัมน์ 1: `"สั่งซื้อ"` = `syncZortPurchases` · `"ซื้อเข้า"` = สร้างในแอป |
| 4 | ZORT ให้ราคามาไหม ฟิลด์ชื่ออะไร | ยิง `GetPurchaseOrders` 30 วัน → dump JSON เต็มของ item ตัวแรก + นับทั้งชุดว่า `pricepernumber > 0` กี่ตัว เทียบกับ `totalprice > 0` |

ปิดท้ายด้วยบล็อก "สรุป" ที่ฟันธงให้เลยว่าต้นเหตุอยู่ฝั่งชีตหรือฝั่ง ZORT

**ทางแยกหลังได้ log** (เตรียมไว้ล่วงหน้า จะได้ไม่ต้องคิดใหม่):

| ผลที่ได้ | ทำต่อ |
|---|---|
| `pricepernumber` มีค่าปกติ | ต้นเหตุอยู่ฝั่งอ่าน/คำนวณ → ไล่ต่อที่ `readPurchases_` / การ์ด "ของเข้าใหม่" |
| `pricepernumber` ว่าง แต่ `totalprice` มี | แก้ `syncZortPurchases` ให้ fallback `totalprice ÷ number` แล้ว re-sync · **KPI หมุนเวียนสินค้าไปต่อได้** |
| ไม่มีราคาทั้ง 2 ฟิลด์ | **ซ่อน KPI หมุนเวียนสินค้า** ตามข้อ 7.3 · การ์ด "ของเข้าใหม่" ตัดบรรทัดมูลค่าออก เหลือ SKU/จำนวนชิ้น · MarginView โชว์ coverage ตามจริง |
| PO ไม่มี line item เลย | `syncZortPurchases` เขียนแถวว่างอยู่ → ต้องดูว่า ZORT ใช้ `PurchaseReceive` แทนหรือเปล่า |

### ⚠️ ก่อนรันได้ ต้องเอาโค้ดเข้า GAS ก่อน
auto-deploy (`.github/workflows/deploy-gas.yml`) trigger เฉพาะ push เข้า **`master`** →
โค้ดที่อยู่บน branch นี้ยังไม่เข้า GAS มี 2 ทาง:
- **(ก) merge เข้า master** — Actions รัน `clasp push` ให้ภายในไม่กี่นาที (ปลอดภัย: ฟังก์ชันนี้อ่านอย่างเดียว
  ไม่มี trigger ไม่มีใครเรียก ไม่กระทบของเดิม)
- **(ข) copy ฟังก์ชันไปวางใน GAS editor ชั่วคราว** แล้วกด Run — ไม่ต้อง merge อะไรเลย
